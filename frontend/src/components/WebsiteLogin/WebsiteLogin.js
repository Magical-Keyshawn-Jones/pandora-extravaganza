import './WebsiteLoginCss.css';
// Components come from here
// Preferred way to import because it uses less space. it doesn't grab the whole library
// import Button from '@mui/material/Button';
// Bootstrap css for bootstrap components
// import 'bootstrap/dist/css/bootstrap.min.css';
import {
    Box,
    Button,
    TextField,
    Container,
    Slider,
  } from '../../Storage/MuiExports';

export default function WebsiteLogin() {
    return (
        <main className='WebsiteLogin'>
            <p> Nothing to show! </p>
            <Button variant='contained'> Cool Button!!! </Button>
        </main>
    )
}